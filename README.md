# fyp
Before running the project, ensure you have Python installed and the following libraries:
pip install flask pymongo streamlit requests python-dotenv

You must run two separate terminals to make the system work:
1. python app.py
2. streamlit run dashboard.py (demo)

Then a browser window will open at http://localhost:8501